# MAISON ไร้สาระ — แผนออกแบบ สร้าง และรันด้วย AI Admin

> สมาคมความไร้สาระชั้นสูง · รับสมาชิกทุกท่าน ยกเว้นไม่รับใคร
> หลักการดำเนินงาน: มนุษย์ (คุณ) ทำหน้าที่เดียวคือ "กดอนุมัติ" — ที่เหลือ Claude Code สร้าง, Cowork บริหาร

---

## 1) Brand System

| องค์ประกอบ | ค่า |
|---|---|
| ชื่อแบรนด์ | MAISON ไร้สาระ (เมซองไร้สาระ) |
| Positioning | Luxury parody — เสิร์ฟความไร้สาระด้วยพิธีรีตองระดับ haute couture |
| โดเมน | maisonraisara.com (สำรอง: raisara.club, maison-raisara.app) |
| LINE OA | @maisonraisara (ซื้อ Premium ID) · ชื่อแสดง "MAISON ไร้สาระ ✦" |
| TikTok | @maison.raisara |
| Facebook | MAISON ไร้สาระ |
| สี | Cream #FFF6E8 · Ink #2E2A45 · Pop #FF4D9D · Sun #FFC33C · Lilac #8C6CFF · Mint #2EC4B6 |
| ฟอนต์ | Mali (display) · Mitr (body) |
| Voice | ภาษาราชสำนักปนบัตเลอร์ เรียกผู้ใช้ว่า "ท่านสมาชิก" ไม่มีอิโมจิยกเว้น ✦ |
| ตัวละครประจำแบรนด์ | "บัตเลอร์" — ตอบทุกอย่างสุภาพเว่อร์ และไม่เคยรีบ |

กติกา copy ที่ AI ทุกตัวต้องยึด (ใส่ใน CLAUDE.md):
- ทุกแอปเรียกว่า "ชิ้นงานในคอลเลกชัน" มีเลข Nº XXX
- สถานะยังไม่เสร็จ = "อยู่ระหว่างรังสรรค์ · ระยะเวลารอ: ชั่วกัลปาวสาน"
- ห้ามขอโทษเกิน 1 ครั้งต่อบทสนทนา บัตเลอร์ไม่ผิด บัตเลอร์แค่ "รับไว้พิจารณา"
- ห้ามพูดถึงเทคโนโลยีเบื้องหลัง (มุกคือทุกอย่างทำ "ด้วยมือช่างฝีมือ")

---

## 2) สถาปัตยกรรมระบบ

```
maison-raisara/  (monorepo — GitHub)
├── CLAUDE.md                # กติกาแบรนด์ + definition of done
├── apps/
│   ├── web/                 # Next.js (React/TSX) → Vercel
│   │   ├── app/n/[id]/      # URL รายแอป เช่น /n/064 (ปุ่มต้องห้าม)
│   │   ├── app/api/og/      # @vercel/og — การ์ดแชร์ผลอัตโนมัติ
│   │   └── lib/apps/        # mini-app ละ 1 ไฟล์ (registry pattern)
│   └── butler/              # Node service → LINE Messaging API webhook
│       ├── liff/            # LIFF wrapper (เว็บเดิมฝังใน LINE)
│       ├── boom.ts          # scheduler เสียงตู้ม ปีละครั้ง (#90)
│       └── persona.ts       # system prompt บัตเลอร์ (Claude API)
├── content/
│   ├── scripts/             # สคริปต์ TikTok ที่ Cowork ร่างไว้รออนุมัติ
│   └── calendar.md          # ปฏิทินคอนเทนต์
└── ops/
    ├── weekly-report/       # รายงานที่ Cowork สรุปทุกจันทร์
    └── inbox-digest/        # สรุปข้อความ LINE ที่บัตเลอร์ตอบไปแล้ว
```

เทคโนโลยี (เลือกให้ต้นทุน ~0 บาทตอนเริ่ม):
- **Frontend:** Next.js บน Vercel free tier (คุณใช้ Vercel อยู่แล้ว) — ทุกแอปย่อยมี URL + OG image ของตัวเอง
- **DB:** Supabase free (เก็บ counter รวมของมุก เช่น ยอดคนกดปุ่มต้องห้ามทั้งโลก, ผู้รอตู้ม, บัญชีรอ) — งานนี้ไม่ต้องใช้ MSSQL
- **LINE:** Messaging API + LIFF · webhook ตอบด้วย Claude API (persona บัตเลอร์) แบบ reply-only เพื่อไม่เสียค่า broadcast
- **Analytics:** Vercel Analytics + นับ event แชร์เอง (ตัวชี้วัดเดียวที่สำคัญ: จำนวนการแชร์การ์ดผล)

---

## 3) Claude Code — ทีมช่างฝีมือ

### CLAUDE.md (หัวใจของ repo)
บรรจุ: brand voice ข้างบน + design tokens + โครง registry ของ mini-app + นิยาม "เสร็จ" =
(1) มี URL /n/XXX (2) มี OG card (3) มีปุ่มแชร์ผล (4) จบใน 1 จอ ไม่มี login (5) ผ่าน build + lint

### Custom commands (.claude/commands/)
| คำสั่ง | ทำอะไร |
|---|---|
| `/new-piece 042` | อ่านไอเดีย Nº 042 จาก registry → สร้าง mini-app เต็มชุด (logic + copy ราชสำนัก + OG card) → เปิด PR |
| `/release` | รวม PR ที่อนุมัติแล้ว → deploy → สร้างประกาศ "เปิดตัวชิ้นงานใหม่" ให้ทุกช่องทาง |
| `/butler-tune` | ทดสอบ persona บัตเลอร์กับชุดคำถามตัวอย่าง ก่อนอัปเดต prompt จริง |
| `/share-card 042` | ปรับดีไซน์การ์ดแชร์ผลของแอปนั้น |

### Sub-agents
- **artisan** — เขียนโค้ด mini-app ตาม registry pattern
- **court-poet** — เกลา copy ทุกชิ้นให้เป็นภาษาราชสำนักตามกติกา (แยกจาก artisan เพื่อไม่ให้ tone หลุด)
- **inspector** — รีวิว PR: เช็ค definition of done + มุกต้องไม่หลุดธีม + ไม่มี login/localStorage

ใช้ผ่าน Multica ที่คุณมีอยู่ได้เลย — ตั้ง job "สร้างชิ้นงานใหม่ทุกสัปดาห์" แล้วให้ผลออกมาเป็น PR รออนุมัติใน Slack

---

## 4) Cowork — บัตเลอร์ใหญ่ (Admin ~100%)

ความจริงที่ต้องพูดตรง ๆ: ระบบที่ดีคือ **Cowork ทำ 99% เหลือ 1% คือนิ้วโป้งคุณ** — งานที่เผยแพร่สู่สาธารณะ (โพสต์, broadcast, deploy production) ควรผ่านการกดอนุมัติเสมอ เพื่อกันมุกหลุดธีมหรือพลาดประเด็นอ่อนไหว ที่เหลือปล่อยอัตโนมัติได้หมด

### Loop รายสัปดาห์ของ Cowork
| วัน | งาน | ระดับอัตโนมัติ |
|---|---|---|
| จันทร์ | ดึง analytics → รายงาน "ราชกิจจานุเบกษาไร้สาระ" (แอปไหนถูกเล่น/แชร์สุด, คน add LINE เท่าไร) | อัตโนมัติเต็ม |
| อังคาร | เลือกไอเดีย Nº ถัดไปจากยอดโหวต "ลงชื่อบัญชีรอ" → สั่ง `/new-piece` ให้ Claude Code | อัตโนมัติเต็ม |
| พุธ | ร่างสคริปต์ TikTok 3 คลิป + แคปชัน FB จากแอปที่กำลังจะเปิดตัว | ร่าง → คุณอนุมัติ |
| พฤหัส | รีวิว PR ร่วมกับ inspector → เตรียม release note | อัตโนมัติ + คุณกด merge |
| ศุกร์ | เปิดตัวชิ้นงานใหม่ (deploy + โพสต์ทุกช่องทาง) | คุณกดปุ่มเดียว |
| ตลอดสัปดาห์ | บัตเลอร์ตอบ LINE/คอมเมนต์อัตโนมัติด้วย persona → Cowork สรุป digest + ธงข้อความที่ควรให้มนุษย์ดู | อัตโนมัติ + escalate |

### เส้นแบ่งที่ตั้งไว้ล่วงหน้า (guardrails)
- บัตเลอร์ LINE: ตอบได้เฉพาะเรื่องแอป/มุกในธีม ถ้าเจอเรื่องจริงจัง (ร้องเรียน, ประเด็นอ่อนไหว, สื่อ/ดีล) → ส่งต่อมนุษย์พร้อมร่างคำตอบ
- ไม่ broadcast เว้นแต่คุณสั่ง (ตู้มปีละครั้งคือ broadcast เดียวที่วางแผนไว้ — และนั่นคือ event ประจำปีของแบรนด์)
- คอนเทนต์ห้ามพาดพิงบุคคลจริง/แบรนด์จริงในทางเสียหาย — inspector เช็คทุกชิ้น

---

## 5) Content Engine (3 เสาหลัก)

1. **Luxury Unboxing** — เปิดตัวชิ้นงานใหม่ทุกศุกร์แบบเปิดกล่องสินค้าหรู (ถุงมือขาว + พื้นกำมะหยี่ + เผยหน้าจอแอปไร้สาระ)
2. **ห้องปฏิบัติการ** — เอาแอปไปทดสอบกับสถานการณ์จริง เช่น โอเคมิเตอร์กับแชทจริง (เบลอชื่อ), จับเวลาแป๊บนึงของคนในบ้าน
3. **จดหมายจากสมาคม** — คอนเทนต์นิ่ง ๆ อ่านประกาศด้วยเสียงทางการ เช่น "ประกาศผลผู้พลาดเสียงตู้มประจำปี 41,003 ท่าน ขอแสดงความเสียใจ"

ทุกคลิปจบด้วย URL รายแอป ไม่ใช่หน้า portal

---

## 6) งบประมาณเริ่มต้น

| รายการ | ราคา |
|---|---|
| โดเมน .com | ~450 บาท/ปี |
| LINE OA Premium ID | ~720 บาท/ปี |
| Vercel + Supabase | 0 (free tier) |
| Claude API (บัตเลอร์ตอบแชท) | ~300–1,000 บาท/เดือน ตามยอดคุย |
| รวมเริ่มต้น | **~2,000 บาทแรก + หลักร้อย/เดือน** |

---

## 7) Roadmap 30 / 60 / 90 วัน

**วัน 1–30 · ก่อตั้งสมาคม**
- จองครบ: โดเมน, @maisonraisara, TikTok, FB (เช็คว่าง → จองวันเดียวให้จบ)
- ตั้ง repo + CLAUDE.md + commands + agents · ย้าย mockup พรีเมี่ยมขึ้น Next.js
- เปิด 8 แอปที่เล่นได้ + URL รายแอป + การ์ดแชร์ผล
- LINE OA พร้อมบัตเลอร์ตอบอัตโนมัติ + LIFF
- Soft launch ด้วย **#90 ห้องรอเสียงตู้ม** เป็นตัวชูโรง (เหตุผลให้คน add LINE ในตัว)

**วัน 31–60 · เข้าจังหวะ**
- Cowork เริ่ม loop รายสัปดาห์เต็มรูปแบบ · เปิดชิ้นงานใหม่ศุกร์ละ 1
- ปุ่ม "ลงชื่อบัญชีรอ" กลายเป็นระบบโหวตเลือกแอปถัดไป (introvert validation)
- คลิปสั้น 3/สัปดาห์ · เริ่มวัดว่าเสาคอนเทนต์ไหน work

**วัน 61–90 · ประเพณีของสมาคม**
- ประกาศ "วันตู้มแห่งชาติ" (ไม่บอกวัน) เป็น event ประจำปี
- ถ้ามี traction: เปิด "Gold Membership 990,000 บาท/เดือน" เป็นปุ่มจริงที่กดแล้วเจอใบสมัคร 47 หน้า (มุกต่อยอด ไม่เก็บเงินจริง)
- รีวิวตัวเลข: ถ้าคนแชร์การ์ดผล > 1,000 ครั้ง/เดือน ค่อยคุยเรื่องต่อยอด (สปอนเซอร์/ของที่ระลึกแบรนด์)

---

## เช็คลิสต์เริ่มพรุ่งนี้
- [ ] เช็ค + จองชื่อครบ 4 แพลตฟอร์ม
- [ ] สร้าง repo `maison-raisara` + วาง CLAUDE.md จากแผนนี้
- [ ] สั่ง Claude Code แปลง mockup → Next.js (งานแรกของ artisan)
- [ ] สมัคร LINE Messaging API + LIFF
- [ ] ตั้ง Cowork loop จันทร์–ศุกร์ตามตาราง
---

## 8) กติกาของ repo (เพิ่มเมื่อ 2026-08-20 · รอบเว็บอย่างเดียว)

### นิยาม "เสร็จ" ของหนึ่งชิ้นงาน
1. เข้าถึงได้ที่ `/n/XXX` และมีรายการใน `lib/registry.ts`
2. มีการ์ดแชร์ที่สะท้อนผลลัพธ์จริง ผ่าน `/api/og?id=XXX&...`
3. มีปุ่มแสดงผลต่อสาธารณชน (Web Share API บนมือถือ · คัดลอกลิงก์บนเดสก์ท็อป)
4. เล่นจบในหนึ่งหน้าจอ ไม่มีการสมัครสมาชิก ไม่ใช้ `localStorage` ไม่ใช้ cookie
5. ผ่าน `pnpm test && pnpm lint && pnpm typecheck && pnpm build`

### สถาปัตยกรรมที่ต้องรักษาไว้
- **ผลลัพธ์อยู่ใน URL เท่านั้น** — ไม่มี state ฝั่งเซิร์ฟเวอร์ ไม่มีฐานข้อมูล
- **แยกตรรกะออกจาก UI** — `NNN.logic.ts` ต้อง pure และห้าม import React
  เพราะทั้งหน้าเว็บและตัวสร้างการ์ดแชร์เรียกใช้ไฟล์เดียวกัน
- **ห้ามส่งข้อความอิสระทั้งก้อนผ่าน URL** — การ์ดมีตราแบรนด์ ถ้าปล่อยให้ใส่ข้อความอะไรก็ได้
  จะมีผู้สร้างการ์ดของสมาคมที่เขียนข้อความเสียหายได้ ส่งได้เฉพาะดัชนีคำวินิจฉัย (`v`)
  ตัวเลขประกอบ (`s`) และข้อความสั้นที่ผ่าน `sanitizeFreeText` แล้ว (`t` ยาวไม่เกิน 40 ตัวอักษร)
- **ตัวเลขยอดรวมยังเป็นการประมาณการ** — `lib/ledger.ts` คำนวณจาก (เลขชิ้นงาน, วันที่)
  ต้องกำกับด้วย `COPY.ledgerNote` เสมอ ห้ามอ้างว่าเป็นยอดเรียลไทม์
  เมื่อต่อฐานข้อมูลจริง ให้เปลี่ยนเฉพาะไส้ในของ `getCount` กับ `increment`

### การ์ดแชร์ใช้ resvg ไม่ใช่ next/og
`next/og` (satori) วางวรรณยุกต์ที่ตามหลังสระบนผิดตำแหน่ง คำว่า "ที่" จะถูกวาดเป็น "ที"
(https://github.com/vercel/satori/issues/668) สมาคมจึงประกอบ SVG เองใน `lib/og-card.ts`
แล้วให้ `@resvg/resvg-js` เป็นผู้เรนเดอร์ เพราะทำ shaping ภาษาไทยถูกต้องครบถ้วน

ผลที่ตามมาที่ต้องรู้:
- จัดวางตำแหน่งเอง ไม่มี flexbox — แก้เลย์เอาต์การ์ดที่ `renderCardSvg`
- ตัดบรรทัดเองด้วย `wrapText` ซึ่งใช้ความกว้างโดยประมาณ ไม่ใช่ความกว้างจริงจากไฟล์ฟอนต์
- ไฟล์ฟอนต์อยู่ที่ `apps/web/assets/fonts` และถูกพาไปกับ deploy ผ่าน `outputFileTracingIncludes`
  ถ้าย้ายโฟลเดอร์ ต้องแก้ `next.config.ts` ด้วย มิฉะนั้นการ์ดจะพังเฉพาะบน production

### ถ้อยคำ
- เรียกผู้ใช้ว่า "ท่านสมาชิก" เสมอ · ไม่มีอิโมจิ ใช้ได้เฉพาะ ✦
- ห้ามพูดถึงเทคโนโลยีเบื้องหลังในถ้อยคำที่ผู้ใช้เห็น
- ห้ามขอโทษเกินหนึ่งครั้งต่อบทสนทนา บัตเลอร์ "รับไว้พิจารณา"
- ห้ามพาดพิงบุคคลจริงหรือแบรนด์จริงในทางเสียหาย

### ยังไม่ได้ทำในรอบนี้
LINE OA · LIFF · บัตเลอร์ตอบแชทด้วย Claude API · Supabase · โดเมนจริง ·
บัญชี TikTok/Facebook · `.claude/commands` และ sub-agents · Cowork weekly loop

### ทิศทางดีไซน์ v2 (2026-08-20)
เปลี่ยนจาก luxury noir/gold เป็น "สมาคมทางการที่ใช้สีลูกกวาด" ตามคำสั่งเจ้าของแบรนด์ —
ขอบหมึกหนา 3px + เงาแข็ง (แบบ neo-brutalism ขี้เล่น) ป้ายเอียง เส้นหยัก จุดลูกกวาด
ถ้อยคำยังทางการเว่อร์เหมือนเดิม มุกอยู่ที่ความขัดกันระหว่างภาษากับหน้าตา
- ชิ้นส่วนมาตรฐาน: `btn-stamp` `btn-quiet` `card-stamp` ใน app/globals.css
- ตรา ✦ บนการ์ดแชร์วาดเป็นข้าวหลามตัดเอง เพราะฟอนต์ Mali ไม่มี glyph นี้
